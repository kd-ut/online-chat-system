<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.chat_backend.modules.message.mapper.MessageMapper">

    <!-- 结果映射 -->
    <resultMap id="UnreadGroupResultMap" type="com.chat.chat_backend.modules.group.dto.response.UnreadGroupVO">
        <result property="fromUserId" column="fromUserId"/>
        <result property="count" column="count"/>
    </resultMap>

    <resultMap id="UnreadMessageDetailResultMap" type="com.chat.chat_backend.modules.message.dto.response.UnreadMessageDetailVO">
        <id property="id" column="id"/>
        <result property="fromUserId" column="fromUserId"/>
        <result property="fromUserNickname" column="fromUserNickname"/>
        <result property="fromUserAvatar" column="fromUserAvatar"/>
        <result property="messageType" column="messageType"/>
        <result property="content" column="content"/>
        <result property="sendTime" column="sendTime"/>
    </resultMap>

    <!-- 查询聊天记录 -->
    <select id="findChatHistory" resultType="com.chat.chat_backend.modules.message.entity.Message">
        SELECT * FROM message
        WHERE (from_user_id = #{userId} AND to_user_id = #{friendId})
           OR (from_user_id = #{friendId} AND to_user_id = #{userId})
        ORDER BY send_time DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 统计聊天记录总数 -->
    <select id="countChatHistory" resultType="java.lang.Long">
        SELECT COUNT(*) FROM message
        WHERE (from_user_id = #{userId} AND to_user_id = #{friendId})
           OR (from_user_id = #{friendId} AND to_user_id = #{userId})
    </select>

    <!-- 统计未读消息总数 -->
    <select id="countUnreadTotal" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM message
        WHERE to_user_id = #{userId} AND is_read = 0 AND recall_time IS NULL
    </select>

    <!-- 按好友分组统计未读消息 -->
    <select id="groupUnreadByFriend" resultMap="UnreadGroupResultMap">
        SELECT from_user_id as fromUserId, COUNT(*) as count
        FROM message
        WHERE to_user_id = #{userId}
          AND is_read = 0
          AND recall_time IS NULL
          AND from_user_id IS NOT NULL
        GROUP BY from_user_id
    </select>

    <!-- 查询未读消息详情 -->
    <select id="findUnreadMessages" resultMap="UnreadMessageDetailResultMap">
        SELECT m.id, m.from_user_id as fromUserId, m.message_type as messageType, m.content, m.send_time as sendTime,
               u.nickname as fromUserNickname, u.avatar as fromUserAvatar
        FROM message m
                 LEFT JOIN user u ON m.from_user_id = u.id
        WHERE m.to_user_id = #{userId}
          AND m.is_read = 0
          AND m.recall_time IS NULL
        ORDER BY m.send_time DESC
    </select>

    <!-- 标记消息为已读 -->
    <update id="markAsRead">
        UPDATE message
        SET is_read = 1, read_time = NOW()
        WHERE to_user_id = #{userId}
          AND from_user_id = #{friendId}
          AND is_read = 0
    </update>

    <!-- 搜索文本消息 -->
    <select id="searchMessages" resultType="com.chat.chat_backend.modules.message.entity.Message">
        SELECT * FROM message
        WHERE (from_user_id = #{userId} OR to_user_id = #{userId})
          AND message_type IN (1, 5, 6)
          AND content LIKE CONCAT('%', #{keyword}, '%')
          AND recall_time IS NULL
        ORDER BY send_time DESC
        LIMIT #{limit}
    </select>

    <!-- 撤回消息（2分钟内） -->
    <update id="recallMessage">
        UPDATE message
        SET recall_time = NOW()
        WHERE id = #{messageId}
          AND from_user_id = #{userId}
          AND send_time > DATE_SUB(NOW(), INTERVAL 2 MINUTE)
    </update>

</mapper>