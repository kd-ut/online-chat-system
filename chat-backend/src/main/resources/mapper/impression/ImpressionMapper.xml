<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.chat_backend.modules.impression.mapper.ImpressionMapper">

    <select id="findImpressionsToUser" resultType="com.chat.chat_backend.modules.impression.entity.Impression">
        SELECT * FROM impression
        WHERE to_user_id = #{userId} AND is_delete = 0
        ORDER BY created_at DESC
    </select>

    <select id="findImpressionsByUser" resultType="com.chat.chat_backend.modules.impression.entity.Impression">
        SELECT * FROM impression
        WHERE from_user_id = #{userId} AND is_delete = 0
        ORDER BY created_at DESC
    </select>

    <select id="findImpressionBetween" resultType="com.chat.chat_backend.modules.impression.entity.Impression">
        SELECT * FROM impression
        WHERE from_user_id = #{fromUserId}
          AND to_user_id = #{toUserId}
          AND is_delete = 0
    </select>

</mapper>